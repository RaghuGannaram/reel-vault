spring.application.name=reelvault

# Server settings
# server.port=8080

# Global logging level
logging.level.root=WARN

# Logging level for Spring framework
logging.level.org.springframework=INFO

# Enable DEBUG logging for Spring Web
logging.level.org.springframework.web=DEBUG

# Enable DEBUG logging for Spring Web Servlet handling
logging.level.org.springframework.web.servlet=DEBUG

# Enable DEBUG logging for HTTP requests
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

# Logging level for the application package
logging.level.com.raghugannaram.reelvault=TRACE

# Logging level for specific class (optional)
# logging.level.com.raghugannaram.reelvault.SomeSpecificClass=TRACE

# MySQL Database settings
# spring.datasource.url=jdbc:mysql://localhost:3306/reelvault?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
# spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# H2 Console settings (if you want to use H2 for testing)
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# Server error settings
# server.error.include-message=always
# server.error.include-binding-errors=always

# JPA settings
# spring.jpa.open-in-view=false

# Thymeleaf settings (if using Thymeleaf)
#spring.thymeleaf.cache=false

# Internationalization settings (if using i18n)
#spring.messages.basename=messages
#spring.messages.cache-duration=3600

# Security settings (if using Spring Security)
#spring.security.user.name=admin
#spring.security.user.password=admin

# Actuator settings (if using Spring Boot Actuator)
#management.endpoints.web.exposure.include=*
#management.endpoint.health.show-details=always
